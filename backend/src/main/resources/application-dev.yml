spring:
    datasource:
        url: jdbc:h2:mem:database
    jpa:
        hibernate:
            ddl-auto: create
        show_sql: true
        properties:
            hibernate:
                format_sql: true
    h2:
        console:
            enabled: true
